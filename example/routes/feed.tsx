import Layout from "../components/Layout"
import React from 'react'

const Feed = () => {
	// const { authenticated, query, redirect } = useContext(PolemicContext)
	// const { items, isLoading, isError } = query()


	return (
		<Layout title='Your Feed'>
			<p>Feed</p>
		</Layout>
	)
}

export default Feed